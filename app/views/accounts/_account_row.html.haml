%tr
  %th{ scope: 'row' }= link_to account.name, account_path(account), title: 'View account transactions',
                                                                    class: 'text-decoration-none'
  %td= account.description
  %td.text-right{class: "#{account.initial_amount >= 0 ? 'positive-number' : 'negative-number'}"}
    = number_to_currency(account.initial_amount)
  %td.text-right{class: "#{account.balance >= 0 ? 'positive-number' : 'negative-number'}"}
    = number_to_currency(account.balance)
  %td
    .action-column
      = link_to '', edit_account_path(account), class: 'bi bi-pencil-fill', title: 'Edit account'
      = link_to '', account_path(account), data: { turbo_method: :delete,
                                                   confirm: 'Are you sure that you want to delete this account with all of its records?' },
                                           class: 'bi bi-trash-fill',
                                           title: 'Delete account'
